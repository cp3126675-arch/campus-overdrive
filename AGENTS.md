# 合成清华小游戏：更新与发布约定

用户要求从 2026-09-07 起严格管理版本，并可回滚。

- 开始修改前检查 Git 状态，保留用户未提交文件。功能更新使用 `codex/` 分支。
- 每次对外更新递增 `public/version.json` 版本，并更新 CHANGELOG.md。区分本地候选版与已验证的公开版。
- 新发布前，保留当前线上原始 ZIP、源码提交、SHA256 和部署提交；不能用后来重新构建的包冒充线上原包。
- 完成 TypeScript、lint、游戏逻辑回归、静态导出和生产构建。测试声明必须与实际证据一致，未实测手机不能宣称真机通过。
- 用 `scripts/release.py` 封存版本源码和产物。版本目录不得覆盖；修复后用新版本号。
- 上传前验证部署包 SHA256；发布后验证工作流成功及线上文件哈希。发布失败时明确保留“待发布”状态。
- 回滚优先从历史部署提交或校验后的旧版 ZIP 恢复；保留新版，不使用 `git reset --hard` 或强制推送。未经用户要求不执行回滚。
- 新闻与人物梗保留可核验出处，区分已证实事实、当事人陈述和游戏虚构。
- 沿用用户授权的 GitHub Pages 发布流程。不得因项目含 hosting.json 改用其他托管服务。

- 长期设计入口为 `docs/游戏设计与长期开发文档.md`，文档导航为 `docs/README.md`。修改规则、内容数量、界面流程或数值时同步对应章节；区分当前实现、已确定方向和候选计划。
- 历史 `docs/清北梗与战斗升级/已实现-*.md` 不作为现行规则；开始任务时核对 GDD 与当前源码，遇到差异明确记录。
- 纯文档修订使用独立 GDD 版本并提交，不递增游戏版本、不重新封存同号游戏产物，也不为文档变更重跑无关游戏构建。检查链接、配置数量、规则和版本信息即可。

操作详情：`docs/版本管理与回滚.md`。

## 开源协作补充（2026-09-07）

- 普通贡献者使用 `npm ci`、`npm run dev`；可选本地 Sites 入口为 `npm run dev:site`。公开源码不包含 `.openai/hosting.json`、凭据或个人未提交文件。
- 贡献指南为 CONTRIBUTING.md；原创代码 MIT，外部图片和音乐遵循 NOTICE.md 中的独立说明，不把未获开放许可的素材标为 MIT。
- PR 检查用 pull_request + contents: read，在托管临时 runner 上运行，不用 pull_request_target 执行贡献者代码；PR 不直接发布线上。
- 音频通过 `public/audio/tracks.json` 登记，校验器只检查结构、签名和大小；不替代来源审核与试听。
- 工程/文档协作更新可独立记录，不改变公开游戏版本或覆盖同号产物；改变游戏体验时仍须新版本、封存和部署核验。
